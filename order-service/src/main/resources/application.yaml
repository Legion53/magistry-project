server:
  port: 8080

spring:
  application:
    name: order-service

inventory:
  base-url: ${INVENTORY_BASE_URL:http://localhost:8081}

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true